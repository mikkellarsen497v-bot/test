# Copy to .env and set values. Do not commit .env.
# Used locally; on Railway/Render/Fly set these in the dashboard.

PORT=3000
NODE_ENV=production

# Optional: use a different directory for JSON data (e.g. persistent volume)
# DATA_DIR=/data

# Optional: Discord webhook URL to post new thread notifications (create channel webhook in Discord)
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# Optional: SAM (GMod admin) MySQL — same DB as your server for live bans on the website
# SAM_MYSQL_HOST=localhost
# SAM_MYSQL_PORT=3306
# SAM_MYSQL_USER=user
# SAM_MYSQL_PASSWORD=secret
# SAM_MYSQL_DATABASE=sam
# SAM_SERVER_NAME=WW2 Imperial Germany RP
