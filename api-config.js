// API base URL: leave empty when the site and API are on the same host (e.g. local or full Node deploy).
// For InfinityFree: set this to your Render (or other) API URL so the frontend calls the API there.
// Example: window.API_BASE = "https://your-app-name.onrender.com";
window.API_BASE = "";
